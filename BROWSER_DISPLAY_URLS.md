# Display URLs

Create display endpoints from the AT Canvas admin interface. Each endpoint gets a permanent URL in the form:

`http://SERVER:8077/display/<token>`

Open that URL fullscreen on any modern browser-capable device. The AT Canvas server handles rendering, layout selection and schedule evaluation. No AT Canvas client software is installed on the display device.
